services:
  - type: web
    name: smartcam-signaling
    runtime: node
    rootDir: signaling
    buildCommand: npm install
    startCommand: node server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
